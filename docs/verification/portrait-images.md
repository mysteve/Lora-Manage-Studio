# 竖向图片展示

日期：2026-09-10。

模型库、收藏、在线发现共用封面改为 2:3 竖向比例；输出结果卡片、详情主图及缩略图同步使用竖向比例。图片采用 contain 完整展示，横图或其他比例的图片留出背景边距。取消模型卡片悬停图片放大，避免裁切边缘。下载行和关联模型的紧凑容器保持原尺寸，图片同样采用完整展示；品牌与关于图片未调整。

本次验证：

- `npm run build`、`npm test`（60 项）及 `git diff --check` 通过，Vite 仍提示主 JS 分块略超 500kB。
- 内置浏览器使用开发示例数据，1440 × 900 检查模型库竖图和横图完整展示；1000 × 800 检查在线发现、输出结果无横向溢出。
- 实际输出图片区域约 219 × 329px，object-fit 为 contain；模型卡片同样为 2:3，控制台无 warning/error。

未实测真实桌面图片、详情弹窗全部交互及运行期间系统减少动态效果切换。
