# 词句组合界面验收

2026-09-10，浏览器开发预览 http://127.0.0.1:1420/?preview，使用手工录入的提示词示例。

- 按 prompt-composer-concept-v1.png 实现紧凑列表、名称编辑、正负向标签页、启用开关、菜单和实时结果。
- 验证添加、就地编辑、片段间插入、拖动排序、键盘方向键排序、菜单上下移动和删除、正负向切换、停用后排除结果、离开页面返回保留内容。
- 1600×1000 双栏和 1000×800 单栏检查，窄窗口无横向溢出；检查减少动态效果模式。
- 完整刷新后控制台无错误或警告，仅 Vite 和 React 开发提示。编辑过程中曾有新增导出尚未写入引发的热更新错误，完整刷新后未复现。
- npm run build 通过；npm test 通过，12 个测试文件、41 项测试，包含跨分组排序隔离和组合语法保留。
- Windows 配置 dragDropEnabled=false，按安装依赖的 Tauri 配置说明启用 HTML5 页面拖放。桌面拖动和桌面剪贴板未实测；此配置在重启桌面应用后生效。
- 草稿仅在本次应用会话内保留，关闭应用后不保存。

截图：prompt-composer-implemented-wide.png、prompt-composer-implemented-narrow.png。

内部滚动调整：连续新增 15 个片段后，1600×1000 窗口的文档高度仍为 1000px，列表可视高度 583px、内容高度 2913px，新增片段自动进入列表可视区域。1000×680 窗口文档高度为 680px，列表与结果保持双栏，片段增多不撑高页面。截图 prompt-composer-scroll-narrow.png。

后续调整：移除片段间、列表底部和菜单中的添加入口，仅保留顶部添加片段按钮。输入文本框常态边框改为 #555b63，悬停加亮，聚焦保持浅绿色。浏览器确认添加按钮数量为 1、片段间入口数量为 0。构建通过。

预设功能验收：移除结果区实时更新状态和底部说明。新增名称保存、选择加载、同名覆盖确认、删除。使用 localStorage 的 lora-studio.prompt-presets.v1，浏览器示例使用独立 .preview 键。保存后完整刷新再加载，确认内容恢复；删除后刷新确认预设移除。测试确认片段名称、正负向、启停、顺序和权重语法持久化，以及损坏数据和存储写入失败处理。构建通过，13 个测试文件共 44 项测试通过。1000×680 页面高度仍为 680px。截图 prompt-presets-narrow.png。未实测桌面应用重启。
