# 资源监测面板层级修复

面板由侧栏内部改为通过 portal 挂到 body，`z-index` 从 95 调整为 400，高于页面、弹窗 100、窗口按钮 200 和提示条 300。采样、暂停、关闭及页面隐藏时清理定时器的逻辑未变。

本次使用 Windows 浏览器开发预览 `http://127.0.0.1:1420/?preview` 验证，不模拟资源数据。

- `npm run build`、`npm test`（28 项）、`git diff --check` 通过。
- 确认面板父节点为 body；关于弹窗打开后，面板三个位置的命中元素均属于面板，截图确认位于弹窗及遮罩上方。
- 1440 × 800 和 1000 × 800 下，面板保持在窗口内且位于最上层。
- 弹窗打开时可以关闭资源面板，弹窗仍保留，随后 Escape 可关闭弹窗。
- 浏览器控制台无警告或错误。

未进行真实桌面资源采样复验、减少动态效果偏好切换或安装包验证。本次没有新增动效或更改采样实现。
